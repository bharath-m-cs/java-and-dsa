E5. Employee(id, department, gender) — per department, count male vs female employees side by side.

select department,

SUM( case when gender ="male" then 1 else 0 end) as male,
SUM( case when gender ="female" then 1 else 0 end) as female

from employee
group by department;